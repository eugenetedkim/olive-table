# File: services/identity-service/.env
# Purpose: Actual environment variable values for local development
# This file should NOT be committed to version control (add to .gitignore)
# Contains actual values that could include secrets

# Same environment variables as in .env.example but with actual values
NODE_ENV=development

# Actual port assigment for the identity service
PORT=3001

# Actual database connection string
# In production, would include authentication credentials and possibly TLS options
DB_CONNECTION=mongodb://mongo:27017/identity

# Actual JWT secret for token signing
# In practice, this should be:
# - At least 32 characters long
# - Randomly generated
# - Unique across environments
# - Stored securely and never committed to to version control
JWT_SECRET=olive_table_dev_secret_key_2025